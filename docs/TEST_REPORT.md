# 系统测试报告

- 实验日期：2026-09-10/11（最近一轮合并后完整实验为第五轮集成版本）
- 被测程序：`build/lab-booking.exe`（正式版）、`build/lab-booking-test.exe`（`TEST_FAULTS` 编译宏）、`build/*-harden.exe`（加固构建）
- 测试脚本：`tests/integration.py`（Python 3 标准库，独立于被测 C 程序）、`tests/unit.c`（Unity 框架单元测试）与 `tests/benchmark.py`（性能基准）
- 总体结论：**单元测试 20/20 通过；合并后完整实验 29 项断言组全部通过**（正常与加固构建各一轮，720 次并发请求零重复占用、20 次中断恢复正确）；浏览器端到端验收通过
- 原始证据目录：`artifacts/test-runs/`（含每轮实验独立数据库与服务器日志，失败亦保留）

## 一、实验环境与边界

- 本机 Windows x64，服务仅监听 `127.0.0.1` 回环地址，每轮实验使用独立临时端口与全新数据库副本。
- 并发实验为固定请求数实验，非长时间压测：1、5、10、20 个并发用户各 20 轮，共 **720 次预约请求**；每轮以线程屏障对齐后同时申请同一空闲场次。
- 中断实验为受控故障注入：测试版程序在指定 `request_id` 的请求处理过程中主动终止进程，共 **20 次**（两类中断点各 10 次），仅针对测试脚本启动的测试进程。
- p50/p95/max 为客户端测量（含连接与线程调度开销），只描述本机本条件，不代表生产容量。
- **工具链限制（如实说明）**：本机 MinGW-w64 GCC 15.2 发行版不含 libasan/libubsan 运行库（已核实无 `libasan.a` 可链接），ASan/UBSan 动态内存检查不可用。替代方案为 `gcc -fanalyzer` 静态分析（第三节）、绑定参数类型静态核查（第四节）与加固构建运行验证（第八节）。

## 二、单元测试（Unity，20 个用例）

`build/unit-tests.exe` 不经 HTTP 直接链接业务层，覆盖三个层次：

| 层次 | 用例 | 结果 |
| --- | --- | --- |
| 纯函数 | `parse_id` 合法/非法/19 位拒绝；`uuid_valid` 格式/大小写/分隔符/长度；`hex_token` 合法/非十六进制/长度不符/空串；`page_arg` 默认值/边界/越界/非数字；`date_start` 含闰年与日历非法值；`date_text` 与 `date_start` 往返一致；哈希确定性与随机数唯一性；响应封套 | 通过 |
| 数据库层 | 种子数据形状（21 用户、3 实验室、场次日边界一致）；`one_booking` 局部唯一索引拦截重复占用；`db_check` 发现"同一用户同场次既有预约又候补"脏数据 | 通过 |
| 业务层（直接调用服务函数） | 预约成功/冲突；替代时段列表与 SQL 对账及同编号重放逐字节一致；候补守卫与 FIFO、退出重入队尾、取消补位；`stats()` 总额与逐日对账；**签到**（未开始拒绝、窗口内成功、同编号重放与重复签到均返回首次时间、非本人 403、已签到不被爽约扫描释放）；**通知**（写入、未读计数、全部标记已读）；**会话列表**（返回 current 标记，含针对绑定格式错误的回归保护）；**强制下线**（成功与重复下线 404）；**改密**（旧密码错误 401、过短/与原密码相同 400、成功后其他会话失效且当前会话保留） | 通过 |

完整输出见 `docs/evidence/unit-tests.txt`。

## 三、静态分析

`scripts/build.ps1 -Analyze` 以 `gcc -fanalyzer` 编译全部自有源码（`src/*.c`、`tests/unit.c`），配合 `-Wall -Wextra -Wformat=2 -Wshadow -Wstrict-prototypes`：**零告警**。开发过程中该配置曾拦截并推动修复一处格式截断告警与两处缩进误导告警。

## 四、绑定参数类型静态核查

在 `db_run/db_rows/db_first/db_num` 的调用中，SQL 占位符个数与格式串（`i`=整数、`s`=文本）必须逐一对应，错位会把整数当指针解引用并导致进程崩溃。本轮新增脚本对所有调用做自动核查：提取 SQL 的 `?` 数量与格式串比对，并按参数形态（字符串字面量 / `now_sec()` / `x.id` / 常量）交叉验证类型。

核查范围：`src/service.c`、`src/http.c`、`src/db.c`、`src/util.c`、`tests/unit.c`，**74 处带绑定参数的调用全部匹配**。该手段在本轮开发中先后定位出两处真实缺陷（`sessions_list` 的查询格式串、`/api/slots` 的格式串漏位），均已修复并补充回归用例。

## 五、功能、安全回归与并发实验（integration.py，24 项）

| 编号 | 内容 | 结果 |
| --- | --- | --- |
| T01–T09 | 登录授权、占用唯一、FIFO 候补与重入队尾、同编号重放/冲突、越权与 CSRF/Origin、畸形与超长输入、过期场次、外部写锁忙碌与重试、禁用候补跳过 | 通过 |
| T13 | 已满场次返回替代时段，列表与 SQL 独立对账一致，首个替代可直接预约 | 通过 |
| T14 | 管理统计总额与逐日数据和 SQL 对账；非管理员 403、非法日期/超 31 天/缺参数 400 | 通过 |
| T15 | 过期会话请求 401；重新登录触发过期会话清理（无残留行） | 通过 |
| T12 | SQLite 完整性、外键、占用与候补唯一性（每档实验后复查） | 通过 |
| T16 | 签到：窗口内成功、同编号重放与重复签到均返回首次时间、非本人 403、`/api/slots` 返回 `checkin_window` 与 `my_checked_in_at` | 通过 |
| T17 | 场次尚未开始与签到窗口已过均返回 409 STATE_CONFLICT，不允许补签 | 通过 |
| T18 | 记录分页：`page/page_size/has_more` 正确，越界 `page=0` 与 `page_size=999` 返回 400，管理端分页生效 | 通过 |
| T19 | 统计含 `no_show/checked_in` 计数；CSV 导出带 BOM、中文表头与合计行；缺参数 400、非管理员 403 | 通过 |
| T20 | 改密：旧密码错误 401、过短或与原密码相同 400；成功后其他会话立即失效、当前会话保留、新密码可登录 | 通过 |
| T21 | 在线会话列表与强制下线；非法会话编号与重复下线 404 | 通过 |
| T22 | 签到超时自动释放为 NO_SHOW、名额按 FIFO 补位、预约人与候补人各收到通知、通知可单条/全部标记已读 | 通过 |

**并发争抢（T10）**：20 个预置用户独立连接同时申请同一空闲场次，每档 20 轮汇总：

| 并发数 | 轮数 | 请求合计 | 成功预约 | `SLOT_FULL` | `DATABASE_BUSY` | p50 中位 (ms) | p95 中位 (ms) | max (ms) |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 20 | 20 | 20 | 0 | 0 | 129.3 | 129.3 | 274.9 |
| 5 | 20 | 100 | 20 | 80 | 0 | 76.9 | 177.6 | 300.8 |
| 10 | 20 | 200 | 20 | 180 | 0 | 97.3 | 280.2 | 390.9 |
| 20 | 20 | 400 | 20 | 380 | 0 | 114.8 | 273.7 | 616.6 |

全部 80 轮中每轮 HTTP 成功数 = 数据库 `CONFIRMED` 占用数 = **1**，其余均收到明确 `SLOT_FULL`(409)，零重复占用、零忙碌失败。本机在测试期间仍有其他进程运行，时间指标仅描述该条件下的客户端观测值。

**中断恢复（T11）**：

| 中断点 | 退出码 | 轮数 | 重启后预约 | 重启后候补 | 原编号重试 |
| --- | ---: | ---: | --- | --- | --- |
| 取消已执行、补位未完成、事务未提交 | 86 | 10 | `CONFIRMED`（整体回滚） | `WAITING`（未跳过） | 首次重试完成取消并补位，再次重放同结果 |
| 事务已提交、HTTP 响应未发送 | 87 | 10 | `CANCELLED` | `PROMOTED` | 返回已保存原结果，不重复执行 |

每轮恢复后同场次有效占用恰为 1，完整性检查通过。该实验验证**进程异常退出**场景（借助 SQLite WAL 与事务原子性），不扩大为断电或存储损坏保证。

## 六、签到与爽约专项实验

**T22** 用 `--checkin-window 1 --sweep-interval 1` 构造确定性场景：用户甲预约后场次进入签到窗口，甲未签到；乙已候补。扫描线程在窗口结束后于单个 `BEGIN IMMEDIATE` 事务内完成：甲预约 → `CANCELLED` 且 `cancel_reason='NO_SHOW'`；乙由 `WAITING` → `PROMOTED` 并新建 `source='WAITLIST'` 的 `CONFIRMED` 预约；向甲写入 `NO_SHOW` 通知、向乙写入 `PROMOTED` 通知；该场次有效占用恒为 1。

**设计要点（本轮修复）**：补位产生的预约若沿用"场次开始时间 + 窗口"判定，会立即再次满足超时条件而陷入反复释放。现改为：`source='WAITLIST'` 的预约以其**补位时刻**作为签到起点，扫描条件与签到接口校验使用同一规则，因此补位后既不会被立即回收，也仍保有完整签到时间。该缺陷由 T22 首次运行时暴露并修复。

## 七、数据模型升级与迁移验证

schema 由 v1 升至 v2：`reservations` 追加 `checked_in_at` 与 `cancel_reason`，`sessions` 追加 `created_at`，新增 `notifications` 表与其索引；`db_check` 增加四条不变式（已签到者必须仍为 CONFIRMED、爽约标记必须伴随 CANCELLED 且未签到、取消原因非空时必须为 CANCELLED、有取消时间者必须有取消原因）。

迁移策略为**按列存在性幂等追加**，不重建表：用 Python 构造一个带数据的 v1 库（含一条历史取消记录、无 `cancel_reason` 列），交由新版程序 `--check` 打开后验证：`user_version` 1→2、三个新列齐全、`notifications` 表建立、历史取消记录回填 `cancel_reason='USER'`、`users`/`slots` 行数不变、完整性检查通过。既有演示库可原地升级，无需重新初始化。

## 八、加固构建运行验证

`scripts/build.ps1 -Harden` 以 `_FORTIFY_SOURCE=3`、`-fstack-protector-strong`、`-ftrivial-auto-var-init=zero`、`-fno-strict-overflow` 重新构建全部程序（`*-harden.exe`）后，单元测试与完整实验全部通过。该组合用于在无 ASan 运行库的条件下提供运行时内存安全防线，覆盖范围弱于完整 sanitizer，报告中不夸大其效力。

## 九、浏览器端到端验证

以 Playwright 驱动真实 Chromium（`127.0.0.1:8770`，独立验收数据库，`--checkin-window 600`），覆盖本轮全部页面改动：

- **用户端**：登录后通知未读徽标显示 1；预约页出现「待签到」高亮卡片并显示签到按钮；点击签到收到「签到成功」且卡片转为「已签到」；我的记录分页控件、预约记录与签到信息渲染正常；通知面板显示补位通知，点击「标为已读」后徽标清零。
- **管理端**：统计表表头为「日期/开放场次/有效预约/已取消/爽约/已签到/候补人数」；点击「导出 CSV」实际下载 `lab-stats-2026-09-10_2026-09-10.csv`；记录分页控件与记录渲染正常。
- 控制台仅 2 条预期错误（favicon 404、未登录时的 401），无其他异常。

截图与导出样本：`docs/evidence/ui-r3/`（01 待签到 → 06 管理端记录，及导出的 CSV 文件）。

## 十、第五轮实验（限流防爆破、运行指标、容量制）

双智能体并行开发（WorkBuddy：CI/基准/指标；ZCode：限流/容量制）合并后重跑，最近一轮完整实验 29 项断言组全部通过（正常与加固构建各一轮，`docs/evidence/results-full-merge*/`）。单元测试扩至 20 个：新增令牌桶（burst 用尽、999ms 不足补充、逐秒补充、闲置不囤积）、登录锁定（阈值触发、锁定内拒绝、到期解锁、成功清零）、容量补位循环（FIFO 连续补位至满员、已持席位再约 409、队列空取消不补位、余位直约）、v2→v3 迁移实测（构造 v2 库重开：列追加、单占用索引退役、user_version=3、旧行默认容量、迁移后完整性通过）、超容量落库必被 db_check 发现。

| 编号 | 内容 | 结果 |
| --- | --- | --- |
| T23 | 同用户名连续错误登录达阈值（--login-max-fails 3）后正确密码也 429 LOGIN_LOCKED；其他账号不受影响；--login-lockout 1 秒后自动解锁 | 通过 |
| T24 | 已登录用户写操作超过 --rate-burst 3 后第 4 次 429 RATE_LIMITED；约 1 秒补充窗口后恢复 | 通过 |
| T25 | /api/admin/metrics 请求计数随请求递增；非管理员 403、匿名 401 | 通过 |
| T26 | 发布 capacity=3 场次：3 席占满后第 4 人 409；列表 capacity/confirmed_count 一致；取消触发 FIFO 补位至 3；已约者再约 409 ALREADY_RESERVED；满员可候补 | 通过 |
| T10 | 并发实验在独立实例关闭限流（--rate-burst 100000）后重跑：80 轮 720 请求仍每轮恰 1 个成功、零忙碌失败 | 通过 |

**容量制语义变更（论文要点）**：单占用唯一索引退役，容量约束改由 BEGIN IMMEDIATE 单写者事务内校验保证——并发正确性论证从"唯一索引兜底"演进为"串行化写事务 + 容量不变量"。v2→v3 幂等迁移经构造的 v2 库实测通过。开发中实测修复两个新缺陷：`publish_slots` 以 int 形参传入 8 字节变参绑定（'i' 格式）导致 CHECK 约束失败、种子初始化失败；RateBucket 以 last_ms==0 兼作初始化哨兵与合法时间戳 0 碰撞，t=0 边界单测暴露后改用显式 started 标志。另发现并修复一处测试自身缺陷（T26 初版丢弃初始预约编号导致误触发 ALREADY_RESERVED）。

**浏览器端到端（第五轮）**：发布容量=3 场次 → 场次卡显示"已约 0/3"；多用户占座后计数实时更新为"已约 3/3 已满"；user04 以过期视图点击 → SLOT_FULL → 替代时段面板正确按容量判空闲 → 一键改约成功；管理端"运行指标"面板渲染请求计数与延迟直方图。截图：`docs/evidence/ui-r5/`。

**性能基准**：`tests/benchmark.py` 独立实验（E1 读性能、E2 同场次 vs 异场次写争用），数据见 `docs/evidence/benchmark/`。

## 十一、其他检查与证据文件

- `--check` 命令行数据库完整性检查：通过；正式版拒绝 `--fault` 系列参数：按预期。
- `docs/evidence/results-full-merge/results.json`——合并后完整实验（29 项，正常构建）；`results-full-merge-harden/`——加固构建；
- `docs/evidence/unit-tests.txt`——单元测试输出；`docs/evidence/benchmark/`——性能基准数据；
- `docs/evidence/ui-r3/`、`ui-r5/`——浏览器验收截图与导出的 CSV；
- `artifacts/test-runs/`——原始数据库与服务器日志（本地保留，不入库）。
